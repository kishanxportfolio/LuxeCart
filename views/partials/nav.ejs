<nav class="scroll top-0 w-full z-[100] bg-[#1e232f] backdrop-blur-md border-b border-white/10 px-6 md:px-10 py-4 transition-all duration-300">
    <div class="max-w-7xl mx-auto flex items-center justify-between">

        <!-- LOGO -->
        <div class="text-2xl md:text-3xl font-black tracking-[0.2em] italic text-white">
            <a href="/" class="hover:text-pink-600 [font-variant:small-caps] transition">Luxe<span class="font-light">Cart</span></a>
        </div>

        <!-- DESKTOP LINKS -->
        <div class="hidden lg:flex items-center space-x-10 text-[10px] font-bold uppercase tracking-[0.3em] text-gray-300">
            <a href="/" class="hover:text-white transition relative group">
                Home
                <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-pink-600 transition-all group-hover:w-full"></span>
            </a>
            <a href="/#about-section" class="hover:text-white transition relative group">
                About
                <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-pink-600 transition-all group-hover:w-full"></span>
            </a>
            <a href="/collection" class="hover:text-white transition relative group">
                Collection
                <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-pink-600 transition-all group-hover:w-full"></span>
            </a>
            <a href="/shop" class="hover:text-white transition relative group">
                Shop
                <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-pink-600 transition-all group-hover:w-full"></span>
            </a>
        </div>

        <!-- RIGHT SIDE -->
        <div class="flex items-center gap-4">

            <!-- SEARCH (desktop only) -->
            <div class="hidden md:relative md:block group">
                <form action="/search" method="GET">
                    <input type="text" name="q" placeholder="SEARCH COLLECTION..."
                        class="bg-white/5 border border-white/10 px-10 py-2 rounded-full text-xs text-white outline-none focus:border-pink-600 focus:w-64 transition-all duration-500 w-48 placeholder:text-gray-500 uppercase tracking-widest">
                    <button type="submit" class="absolute left-4 top-1/2 -translate-y-1/2 outline-none">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-gray-400 group-focus-within:text-pink-600 transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35M10 18a8 8 0 100-16 8 8 0 000 16z" />
                        </svg>
                    </button>
                </form>
            </div>

            <!-- LOGIN/USER (desktop only) -->
            <div class="hidden sm:flex items-center gap-3">
                <% if (user) { %>
                    <div class="flex items-center gap-4">
                        <a href="/profile" class="group flex items-center gap-3">
                            <div class="relative">
                                <div class="w-9 h-9 rounded-full bg-zinc-900 flex items-center justify-center text-[11px] font-black text-amber-500 border border-amber-500/50 group-hover:border-amber-400 transition-all shadow-[0_0_15px_rgba(245,158,11,0.2)]">
                                    <%= user.name.charAt(0).toUpperCase() %>
                                </div>
                                <span class="absolute bottom-0 right-0 w-2.5 h-2.5 bg-amber-500 border-2 border-[#1e232f] rounded-full"></span>
                            </div>
                            <div class="hidden md:flex flex-col items-start leading-none">
                                <div class="flex items-center gap-1">
                                    <i class="ri-user-3-line text-zinc-500 text-xs group-hover:text-amber-500 transition-colors"></i>
                                </div>
                                <span class="text-[9px] text-zinc-500 font-bold uppercase tracking-tighter mt-1 group-hover:text-amber-500 transition-colors">
                                    <%= user.name.split(' ')[0] %>
                                </span>
                            </div>
                        </a>
                        <a href="/logout"
                            class="flex items-center gap-2 px-4 py-2 border border-zinc-800 rounded-full text-[10px] font-black text-zinc-500 uppercase tracking-widest hover:border-red-500/50 hover:text-red-500 hover:bg-red-500/5 transition-all duration-300 active:scale-95">
                            <i class="ri-logout-circle-r-line text-sm"></i>
                            Logout
                        </a>
                    </div>
                <% } else { %>
                    <a href="/login" class="text-[10px] font-bold text-white uppercase tracking-widest hover:text-pink-600 transition">Login</a>
                    <a href="/signup" class="bg-white text-black px-6 py-2.5 rounded-full text-[10px] font-bold uppercase tracking-widest hover:bg-pink-600 hover:text-white transition-all shadow-xl">Sign Up</a>
                <% } %>
            </div>

            <!-- ✅ SIRF EK HAMBURGER - mobile pe dikhega -->
            <button onclick="toggleMenu()" class="lg:hidden flex flex-col gap-[5px] p-2 focus:outline-none">
                <span class="w-6 h-[2px] bg-white rounded-full transition-all duration-300"></span>
                <span class="w-4 h-[2px] bg-white rounded-full transition-all duration-300"></span>
                <span class="w-6 h-[2px] bg-white rounded-full transition-all duration-300"></span>
            </button>

        </div>
    </div>
</nav>

<!-- MOBILE DRAWER -->
<div id="mobileMenu"
    class="fixed top-0 right-0 h-screen w-[80%] max-w-[320px] z-[99999]
    transform translate-x-full transition-transform duration-500 ease-[cubic-bezier(0.4,0,0.2,1)]
    flex flex-col shadow-[-20px_0_60px_rgba(0,0,0,0.4)]">

    <!-- DARK HEADER -->
    <div class="bg-black px-6 pt-10 pb-8 flex flex-col gap-1">
        <div class="flex items-center justify-between mb-6">
            <span class="text-white font-black text-xl tracking-tighter italic">Luxe<span class="text-pink-500">Cart</span></span>
            <button onclick="toggleMenu()"
                class="w-9 h-9 rounded-full border border-white/20 flex items-center justify-center text-white hover:border-pink-500 hover:text-pink-500 transition-all">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" viewBox="0 0 24 24">
                    <path d="M18 6L6 18M6 6l12 12"/>
                </svg>
            </button>
        </div>
        <p class="text-gray-400 text-[10px] uppercase tracking-[0.25em] font-semibold">Luxury Fashion Destination</p>
    </div>

    <!-- LIGHT LINKS -->
    <!-- LIGHT LINKS wali div ko replace karo is se -->
<div class="bg-[#0f1117] flex-1 flex flex-col px-6 py-6 gap-1 overflow-y-auto">

    <a href="/" onclick="toggleMenu()" class="group flex items-center justify-between py-4 border-b border-white/10">
        <span class="text-[15px] font-bold uppercase tracking-widest text-gray-300 group-hover:text-pink-500 transition-all">Home</span>
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" viewBox="0 0 24 24" class="text-white/20 group-hover:text-pink-500 group-hover:translate-x-1 transition-all duration-200"><path d="M9 18l6-6-6-6"/></svg>
    </a>

    <a href="/shop" onclick="toggleMenu()" class="group flex items-center justify-between py-4 border-b border-white/10">
        <span class="text-[15px] font-bold uppercase tracking-widest text-gray-300 group-hover:text-pink-500 transition-all">Shop All</span>
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" viewBox="0 0 24 24" class="text-white/20 group-hover:text-pink-500 group-hover:translate-x-1 transition-all duration-200"><path d="M9 18l6-6-6-6"/></svg>
    </a>

    <a href="/collection" onclick="toggleMenu()" class="group flex items-center justify-between py-4 border-b border-white/10">
        <span class="text-[15px] font-bold uppercase tracking-widest text-gray-300 group-hover:text-pink-500 transition-all">Collection</span>
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" viewBox="0 0 24 24" class="text-white/20 group-hover:text-pink-500 group-hover:translate-x-1 transition-all duration-200"><path d="M9 18l6-6-6-6"/></svg>
    </a>

    <a href="/cart" onclick="toggleMenu()" class="group flex items-center justify-between py-4 border-b border-white/10">
        <span class="text-[15px] font-bold uppercase tracking-widest text-gray-300 group-hover:text-pink-500 transition-all">Cart</span>
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" viewBox="0 0 24 24" class="text-white/20 group-hover:text-pink-500 group-hover:translate-x-1 transition-all duration-200"><path d="M9 18l6-6-6-6"/></svg>
    </a>

    <!-- BOTTOM BUTTONS -->
    <div class="mt-auto pt-8 flex flex-col gap-3">
        <a href="/login"
            class="text-center bg-pink-600 text-white py-3.5 rounded-xl font-black text-[11px] uppercase tracking-[0.2em] hover:bg-pink-500 transition-all duration-300 shadow-[0_0_20px_rgba(219,39,119,0.4)]">
            Login Member
        </a>
        <a href="/signup"
            class="text-center border border-white/20 text-gray-300 py-3.5 rounded-xl font-black text-[11px] uppercase tracking-[0.2em] hover:border-pink-500 hover:text-pink-500 transition-all duration-300">
            Create Account
        </a>
    </div>
</div>
</div>

<!-- OVERLAY -->
<div id="menuOverlay" onclick="toggleMenu()"
    class="fixed inset-0 bg-black/60 backdrop-blur-sm opacity-0 pointer-events-none transition-opacity duration-500 z-[99998]">
</div>

<script>
    function toggleMenu() {
        const menu = document.getElementById('mobileMenu');
        const overlay = document.getElementById('menuOverlay');
        const isOpen = !menu.classList.contains('translate-x-full');
        if (isOpen) {
            menu.classList.add('translate-x-full');
            overlay.classList.add('opacity-0', 'pointer-events-none');
            overlay.classList.remove('opacity-100');
            document.body.style.overflow = 'auto';
        } else {
            menu.classList.remove('translate-x-full');
            overlay.classList.remove('opacity-0', 'pointer-events-none');
            overlay.classList.add('opacity-100');
            document.body.style.overflow = 'hidden';
        }
    }
</script>